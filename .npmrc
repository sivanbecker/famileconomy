legacy-peer-deps=true

# Audit ignorelist: known CVEs in development tooling (Expo 51, Next.js).
# These are resolved in Expo 55+ and Next.js 17+. Blocking to avoid breaking CI.
# See .trivyignore for details.
audit-level=moderate

# Allowed HIGH severity: pre-existing Expo + Next.js dev tooling CVEs
# CVE-2024-27289 (react-native)
# CVE-2025-24951, CVE-2025-24952, CVE-2025-24953 (fast-xml-parser via Expo)
# GHSA-qx2v-qp2m-jg93 (postcss)
# GHSA-m6fv-jmcg-4jfg (send)
# GHSA-* (node-tar) - multiple tar CVEs in Expo CLI
# Future: upgrade to Expo 55+ and Next.js 17+ to resolve
